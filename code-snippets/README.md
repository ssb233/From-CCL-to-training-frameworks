# Code Snippets

This directory stores small, focused experiments.

Examples planned:

- Minimal `torch.distributed` launch
- `all_reduce` demo
- `all_gather` demo
- `reduce_scatter` demo
- DDP gradient synchronization demo
- Simple ZeRO-style sharding sketch

Keep snippets short. If an experiment becomes a real project, move it to a
separate directory with its own README.

